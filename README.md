
  # Memory Diary Layout

  This is a code bundle for Memory Diary Layout. The original project is available at https://www.figma.com/design/s6x5eN70nD1x0oGK4GKoWo/Memory-Diary-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  